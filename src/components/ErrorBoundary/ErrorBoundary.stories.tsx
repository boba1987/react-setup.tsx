/* eslint-disable */
import React from 'react';
import ErrorBoundary from './ErrorBoundary';

export default {
  title: "ErrorBoundary",
};

export const Default = () => <ErrorBoundary />;

Default.story = {
  name: 'default',
};
